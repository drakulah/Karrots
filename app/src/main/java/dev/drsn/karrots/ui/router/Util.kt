package dev.drsn.karrots.ui.router

//const val SCREEN_ROUTE_PERM_PATH = "permissionValidator"
const val SCREEN_ROUTE_INTERACTION_PATH = "interaction"

//fun String?.isRoutePermissionValidator(): Boolean {
//	val route = SCREEN_ROUTE_PERM_PATH
//	if (this.isNullOrEmpty() || this.length < route.length) return false
//	val partialString = this.slice(route.indices).lowercase()
//	return partialString == route.lowercase()
//}
//
//fun String?.isRouteInteraction(): Boolean {
//	val route = SCREEN_ROUTE_INTERACTION_PATH
//	if (this.isNullOrEmpty() || this.length < route.length) return false
//	val partialString = this.slice(route.indices).lowercase()
//	return partialString == route.lowercase()
//}